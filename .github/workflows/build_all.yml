name: Build all PDF

on:
  push:  # comment this line if you dont want to build on every push.
  workflow_dispatch:

jobs:
  discover-and-build:
    runs-on: ubuntu-latest
    outputs:
      projects: ${{ steps.discover.outputs.projects }}
      has_projects: ${{ steps.discover.outputs.has_projects }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Discover LaTeX projects
        id: discover
        run: |
          echo "🔍 Searching for LaTeX projects..."
          projects=$(find . -mindepth 2 -maxdepth 2 -name main.tex -printf '%h\n' || true)
          projects=$(echo "$projects" | sed '/^\s*$/d')
          if [ -z "$projects" ]; then
            echo "⚠️ No LaTeX projects found!"
            echo "projects=[]" >> $GITHUB_OUTPUT
            echo "has_projects=false" >> $GITHUB_OUTPUT
          else
            echo "✅ Found projects:"
            echo "$projects"
            projects_json=$(echo "$projects" | jq -R -s -c 'split("\n")[:-1]')
            echo "projects=$projects_json" >> $GITHUB_OUTPUT
            echo "has_projects=true" >> $GITHUB_OUTPUT
          fi

  build:
    needs: discover-and-build
    if: ${{ needs.discover.outputs.has_projects == 'true' }}
    runs-on: ubuntu-latest
    env:
      DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
    strategy:
      fail-fast: false
      matrix:
        project: ${{ fromJson(needs.discover.outputs.projects) }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get commit info
        id: commit
        run: |
          echo "message=$(git log -1 --pretty=%B)" >> $GITHUB_OUTPUT
          echo "author=$(git log -1 --pretty=%an)" >> $GITHUB_OUTPUT

      - name: Compile LaTeX project
        uses: xu-cheng/latex-action@v4
        with:
          root_file: main.tex
          working_directory: ${{ matrix.project }}

      - name: Upload PDF as artifact
        run: |
          # Clean the project name (remove leading ./ and replace / with _)
          SAFE_NAME=$(echo "${{ matrix.project }}" | sed 's|^\./||; s|/|_|g')
          echo "Uploading artifact: ${SAFE_NAME}-pdf"
          echo "path: ${{ matrix.project }}/main.pdf"

          # Use the cleaned name for upload
          echo "name=${SAFE_NAME}-pdf" >> $GITHUB_ENV
        shell: bash

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ env.name }}
          path: ${{ matrix.project }}/main.pdf

      - name: Send PDF to Discord
        if: ${{ env.DISCORD_WEBHOOK != '' }}
        env:
          DISCORD_WEBHOOK: ${{ env.DISCORD_WEBHOOK }}
          WORKDIR: ${{ matrix.project }}
          REPO_NAME: ${{ github.event.repository.name }}
          COMMIT_MESSAGE: ${{ steps.commit.outputs.message }}
          COMMIT_AUTHOR: ${{ steps.commit.outputs.author }}
          BRANCH_NAME: ${{ github.ref_name }}
        run: |
          CONTENT=$(jq -n \
            --arg repo "$REPO_NAME" \
            --arg wdir "$WORKDIR" \
            --arg author "$COMMIT_AUTHOR" \
            --arg msg "$COMMIT_MESSAGE" \
            --arg branch "$BRANCH_NAME" \
            '{content: "# 📙 **\($repo)**\n- Built **\($wdir)/main.tex** from branch `\($branch)`\n🧾 Last Commit by **\($author)**:\n```\n\($msg)\n```"}')

          curl -F "payload_json=${CONTENT}" \
               -F "file=@${WORKDIR}/main.pdf" \
               "$DISCORD_WEBHOOK"